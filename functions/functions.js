// let globalData = ""; // Глобальная переменная для хранения данных

// exports.handler = async function(event, context) {
//   const newData = JSON.parse(event.body);

//   // Сохраните данные в глобальную переменную
//   globalData = newData;

//   // Ответите клиенту с кодом 200, чтобы показать успешное обновление данных
//   return {
//     statusCode: 200,
//     body: "Данные успешно обновлены"
//   };
// };
